 // Este es un ejemplo de programa Mini -C

 int main () {
/* Declaracion de variables */
int a = 10;
float b = 20.5;
 int resultado ;

 Operaciones aritmeticas
 resultado = a + ( int )b * 2;



 if ( resultado >= 50 && a != b ) {
 return 1; // Retorna exito
 } else {
 while (a < 20) {
 a = a + 1; // Incrementa a
}
 }

float pi_value = 3.14159;
for (int i = 0; i < 5; i ++) {
// Un bucle for
    }

    return 0; // Fin del programa
}